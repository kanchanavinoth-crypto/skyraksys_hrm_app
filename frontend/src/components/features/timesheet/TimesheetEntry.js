import React from 'react';
import EnhancedTimesheetEntry from './EnhancedTimesheetEntry';

/**
 * TimesheetEntry Component
 * 
 * This is a wrapper component that renders the EnhancedTimesheetEntry component
 * with both timesheet entry and history functionality.
 */
const TimesheetEntry = () => {
  return <EnhancedTimesheetEntry />;
};

export default TimesheetEntry;
