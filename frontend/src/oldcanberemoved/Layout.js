/**
 * Layout Component - Main Entry Point
 * Redirects to the layout implementation for better organization
 */

import React from 'react';
import LayoutMain from './layout/Layout';

const Layout = () => {
  return <LayoutMain />;
};

export default Layout;